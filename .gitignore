# Build output (root binary only, not the source directory)
/agenthub

# Test coverage
coverage.out
coverage.html

# Secrets / encrypted store
*.enc

# Local Claude Code config
.claude/
.mcp.json

# OS
.DS_Store
